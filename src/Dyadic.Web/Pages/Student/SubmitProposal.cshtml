@page
@model Dyadic.Web.Pages.Student.SubmitProposalModel
@{
    ViewData["Title"] = "Submit Proposal";
}

<div class="row justify-content-center">
    <div class="col-12 col-md-8">
        <h2 class="mb-4">Submit Your Research Proposal</h2>

        @if (Model.IsSubmitted)
        {
            <div class="alert alert-success">
                Your proposal has been submitted and is under review.
            </div>
            <dl class="row">
                <dt class="col-sm-3">Title</dt>
                <dd class="col-sm-9">@Model.Input.Title</dd>
                <dt class="col-sm-3">Abstract</dt>
                <dd class="col-sm-9">@Model.Input.Abstract</dd>
                <dt class="col-sm-3">Tech Stack</dt>
                <dd class="col-sm-9">@Model.Input.TechStack</dd>
            </dl>
        }
        else
        {
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <div class="d-flex justify-content-between">
                        <label asp-for="Input.Title" class="form-label fw-semibold"></label>
                        <small class="text-muted"><span id="titleCount">200</span> characters remaining</small>
                    </div>
                    <input asp-for="Input.Title" class="form-control form-control-lg" maxlength="200" id="titleInput" />
                    <span asp-validation-for="Input.Title" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <div class="d-flex justify-content-between">
                        <label asp-for="Input.Abstract" class="form-label fw-semibold"></label>
                        <small class="text-muted"><span id="abstractCount">2000</span> characters remaining</small>
                    </div>
                    <textarea asp-for="Input.Abstract" class="form-control" rows="8" maxlength="2000" id="abstractInput"></textarea>
                    <span asp-validation-for="Input.Abstract" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <div class="d-flex justify-content-between">
                        <label asp-for="Input.TechStack" class="form-label fw-semibold"></label>
                        <small class="text-muted"><span id="techCount">500</span> characters remaining</small>
                    </div>
                    <input asp-for="Input.TechStack" class="form-control" maxlength="500" id="techInput"
                           placeholder="e.g. ASP.NET Core, React, SQL Server" />
                    <span asp-validation-for="Input.TechStack" class="text-danger small"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="Input.ResearchAreaId" class="form-label fw-semibold"></label>
                    <select asp-for="Input.ResearchAreaId" asp-items="Model.ResearchAreaOptions" class="form-select">
                        <option value="">— Select a research area —</option>
                    </select>
                    <span asp-validation-for="Input.ResearchAreaId" class="text-danger small"></span>
                </div>

                <div class="d-flex gap-3">
                    <button type="submit" asp-page-handler="Draft" class="btn btn-outline-secondary btn-lg">Save as Draft</button>
                    <button type="submit" asp-page-handler="Submit" class="btn btn-success btn-lg">Submit Proposal</button>
                    <a asp-page="/Student/MyProposal" class="btn btn-outline-secondary btn-lg">Cancel</a>
                </div>
            </form>
        }
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function updateCount(inputId, countId, max) {
            const input = document.getElementById(inputId);
            const count = document.getElementById(countId);
            if (input && count) {
                const refresh = () => { count.textContent = max - input.value.length; };
                refresh();
                input.addEventListener('input', refresh);
            }
        }
        document.addEventListener('DOMContentLoaded', () => {
            updateCount('titleInput', 'titleCount', 200);
            updateCount('abstractInput', 'abstractCount', 2000);
            updateCount('techInput', 'techCount', 500);
        });
    </script>
}
